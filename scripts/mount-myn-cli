#!/usr/bin/env bash
set -euo pipefail

command_name="mount-myn-cli"
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(git -C "$script_dir" rev-parse --show-toplevel)"
cli_dir="$repo_root/go/cli"
bin_dir="${MYN_DEV_BIN_DIR:-$HOME/.local/bin}"
launcher_name="${MYN_DEV_LAUNCHER_NAME:-myn}"
force=0
unmount=0
marker="# generated by scripts/mount-myn-cli"

usage() {
  cat <<'USAGE'
Usage: scripts/mount-myn-cli [--bin-dir DIR] [--name NAME] [--force] [--unmount]

Creates a local launcher for the Go `myn` CLI dev build. The launcher builds
and runs this checkout without changing the command's invocation directory, so
source changes are picked up automatically and cwd-sensitive commands behave
like the installed CLI.

Options:
  --bin-dir DIR  Directory for the launcher, default: ${MYN_DEV_BIN_DIR:-$HOME/.local/bin}
  --name NAME    Launcher name, default: ${MYN_DEV_LAUNCHER_NAME:-myn}
  --force        Replace an existing file at the launcher path
  --unmount      Remove a launcher previously created by this script
  -h, --help     Show this help

Environment:
  MYN_DEV_BIN_DIR        Default launcher directory
  MYN_DEV_LAUNCHER_NAME  Default launcher name
USAGE
}

die() {
  echo "$command_name: $*" >&2
  exit 1
}

while [ "$#" -gt 0 ]; do
  case "$1" in
    --bin-dir)
      [ "$#" -ge 2 ] || die "--bin-dir requires a directory"
      bin_dir="$2"
      shift 2
      ;;
    --name)
      [ "$#" -ge 2 ] || die "--name requires a launcher name"
      launcher_name="$2"
      shift 2
      ;;
    --force)
      force=1
      shift
      ;;
    --unmount)
      unmount=1
      shift
      ;;
    --help|-h)
      usage
      exit 0
      ;;
    *)
      die "unknown argument: $1"
      ;;
  esac
done

[ -d "$cli_dir" ] || die "Go CLI directory not found: $cli_dir"
[ -f "$cli_dir/cmd/myn/main.go" ] || die "Go CLI entrypoint not found: $cli_dir/cmd/myn/main.go"
[ -n "$launcher_name" ] || die "--name cannot be empty"
case "$launcher_name" in
  */*) die "--name must be a file name, not a path" ;;
esac

launcher_path="$bin_dir/$launcher_name"

if [ "$unmount" -eq 1 ]; then
  if [ ! -e "$launcher_path" ]; then
    echo "$launcher_path is not mounted"
    exit 0
  fi
  if ! grep -Fq "$marker" "$launcher_path" 2>/dev/null; then
    die "refusing to remove $launcher_path because it was not created by this script"
  fi
  rm -f -- "$launcher_path"
  echo "Removed $launcher_path"
  exit 0
fi

mkdir -p -- "$bin_dir"

if [ -e "$launcher_path" ] && [ "$force" -ne 1 ]; then
  if ! grep -Fq "$marker" "$launcher_path" 2>/dev/null; then
    die "$launcher_path already exists; rerun with --force to replace it"
  fi
fi

printf -v quoted_cli_dir '%q' "$cli_dir"
tmp_path="$(mktemp "$bin_dir/.$launcher_name.tmp.XXXXXX")"
{
  printf '%s\n' '#!/usr/bin/env bash'
  printf '%s\n' 'set -euo pipefail'
  printf '%s\n' "$marker"
  printf 'cli_dir=%s\n' "$quoted_cli_dir"
  printf '%s\n' 'tmp_dir="${TMPDIR:-/tmp}/myn-dev-cli"'
  printf '%s\n' 'mkdir -p -- "$tmp_dir"'
  printf '%s\n' 'binary="$(mktemp "$tmp_dir/myn.XXXXXX")"'
  printf '%s\n' 'cleanup() { rm -f -- "$binary"; }'
  printf '%s\n' 'trap cleanup EXIT'
  printf '%s\n' '('
  printf '%s\n' '  cd -- "$cli_dir"'
  printf '%s\n' '  go build -o "$binary" ./cmd/myn'
  printf '%s\n' ')'
  printf '%s\n' '"$binary" "$@"'
} > "$tmp_path"
chmod 755 "$tmp_path"
mv -f -- "$tmp_path" "$launcher_path"

echo "Mounted $launcher_name at $launcher_path"
if ! command -v "$launcher_name" >/dev/null 2>&1; then
  echo "Add $bin_dir to PATH to run '$launcher_name' from anywhere."
elif [ "$(command -v "$launcher_name")" != "$launcher_path" ]; then
  echo "Note: PATH currently resolves '$launcher_name' to $(command -v "$launcher_name"), not $launcher_path."
fi
